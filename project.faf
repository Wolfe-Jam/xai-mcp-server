faf_version: "2.0"
generated: 2025-11-28

project:
  name: xai-mcp-server
  goal: Rust MCP server for FAF scoring and bi-sync healing
  type: backend-api
  main_language: Rust

stack:
  runtime: tokio
  backend: mcp-protocol
  build: cargo
  database: ~
  hosting: local-stdio

human_context:
  who: wolfejam for xAI/Grok integration
  what: Championship-grade Rust MCP engine demonstrating FAF capabilities
  why: Native Grok loader with Glass Hood transparent scoring
  where: xAI exclusive, FAF Foundation IP
  when: 2025-11-24 delivery
  how: cargo build --release && cargo run

ai_tldr: |
  Rust MCP server for FAF. Tools: faf_score_aligned (Glass Hood scoring),
  bi_sync (CLAUDE.md ↔ project.faf healing), resource faf://project/dna.
  Weights: [0.40, 0.35, 0.15, 0.10]. 300/300 WJTTC tests passing.
  Sub-10ms context switching. One-click demo in <30 seconds.
